# Example environment variables for development - DO NOT commit real secrets
# Copy this file to `.env` and fill the values locally (do NOT push your .env)

ADMIN_PASSWORD=change_me_admin_password
MASTER_ACTIVATION_KEY=change_me_master_key
COOKIE_PASSWORD=change_me_cookie_password

# Optional: other runtime configuration
# STREAMLIT_SERVER_PORT=8501
